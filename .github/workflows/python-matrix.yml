name: Python Lint

on:
  push: 
    branches[main]

jobs:
  Validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
        
      - name: Install Dependencies
        uses: actions/checkout@v4
        
      - name:
